version: "3.11"

services:
  app:
    build: .                 # construit l’image à partir du Dockerfile
    image: hello-node        # nom local de l’image (facultatif mais pratique)
    container_name: hello-node

    # ports:                 # inutile ici – pas de serveur HTTP
    #   - "3000:3000"
    # volumes:               # exemple pour recharger le code sans rebuild
    #   - ./:/app
